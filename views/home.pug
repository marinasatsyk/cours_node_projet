extends layouts/frontend

include includes/_mixins

block head
  //- Inject here JS and styles for this specific page
  //- script(src="../utils/utils.js") 

block content
  .ui.middle.aligned.center.aligned.grid
    .sixteen.wide.ten.wide.tablet.seven.wide.computer.column
      h1 DASHBOARD
      - function strUcFirst(a){return (a+'').charAt(0).toUpperCase()+a.substr(1);}
      - const modifiedFirstname = strUcFirst(user.firstName);
      h3 Hello #{modifiedFirstname}



      